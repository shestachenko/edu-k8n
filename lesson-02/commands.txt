Run 10 Nginx containers and assign ports:

for i in $(seq 1 10); do
  host_port=$((8080 + i))
  docker run -d --name "nginx-$i" -p "${host_port}:80" nginx:alpine
done

Stop and remove all containers:

docker rm -f $(docker ps -aq --filter "name=^nginx-")

