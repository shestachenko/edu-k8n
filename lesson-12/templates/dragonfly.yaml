apiVersion: dragonflydb.io/v1alpha1
kind: Dragonfly
metadata:
  name: edu-dragonfly
  namespace: {{ include "course-app.namespace" . }}
spec:
  replicas: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: "1"
      memory: 512Mi
